version: "3"

tasks:
  setup:
    status:
      - test -n "$(conda env list | grep 'em-es.sim')"
    cmds:
      - mamba env create -f environment.yaml
    internal: true

  run:
    deps: [setup]
    env:
      RP_URI: "{{.LOCALHOST}}:9092"
      SR_URI: "http://{{.LOCALHOST}}:9080"
      CONFIG_DIR: "{{.CONFIG_DIR}}"
    cmds:
      - mamba run -n em-es.sim python src/main.py
