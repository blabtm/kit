networks:
  v2k:
    external: true

volumes:
  appsmith:
    external: true

services:
  appsmith:
    image: appsmith/appsmith-ee:latest
    ports:
      - target: 80
        published: $APPSMITH_PORT
        mode: ingress
    volumes:
      - appsmith:/appsmith-stacks
    networks:
      - v2k
